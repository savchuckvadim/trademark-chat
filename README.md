# High-Performance AI Chat Interface

Высокопроизводительный интерфейс чата для LLM-платформы с поддержкой потоковой генерации текста, оптимизированным рендерингом и поддержкой Markdown.

## 📋 Описание

Проект представляет собой SPA-приложение, эмулирующее общение с ИИ. Основная задача — обеспечить плавную работу интерфейса (60 FPS) при обработке больших объемов текстовых данных (History > 5MB) и высокоскоростном потоковом обновлении ответов без блокировки UI.

## 🚀 Быстрый старт

### Требования

- Node.js 18+
- npm/pnpm/yarn/bun

### Установка

```bash
# Установка зависимостей
npm install
```

### Запуск

```bash
# Запуск dev-сервера
npm run dev

# Сборка для production
npm run build

# Просмотр production сборки
npm run preview

# Линтинг
npm run lint

# Форматирование кода
npm run format
```

Приложение будет доступно по адресу `http://localhost:5173` (или другой порт, если 5173 занят).

## 🏗️ Архитектура проекта

Проект использует **Feature-Sliced Design (FSD)** архитектуру для организации кода.

### Структура директорий

```
src/
├── modules/
│   ├── app/                    # Инициализация приложения
│   │   ├── model/             # Глобальные стейты
│   │   └── ui/                # Корневой компонент App
│   │
│   ├── entities/              # Бизнес-сущности
│   │   └── message/           # Сущность сообщения
│   │       ├── hooks/         # Хуки для работы с сообщениями
│   │       ├── lib/           # Утилиты (Markdown парсер)
│   │       ├── model/         # Zustand store для сообщений
│   │       ├── tpyes/         # TypeScript типы
│   │       └── ui/             # UI компоненты сообщений
│   │
│   ├── features/              # Функциональные возможности
│   │   └── chat/              # Функционал чата
│   │       ├── hooks/         # Бизнес-логика (стриминг, форма)
│   │       ├── lib/           # Утилиты (генератор текста)
│   │       ├── model/         # Zustand store для чата
│   │       ├── services/      # Сервисы (ChatStreamService)
│   │       └── ui/            # UI компоненты чата
│   │
│   ├── shared/                # Переиспользуемый код
│   │   ├── components/        # UI компоненты (Button, Card)
│   │   └── lib/               # Утилиты (cn, utils)
│   │
│   └── widgetes/              # Композитные компоненты
│       └── chat/              # Виджет чата (MessageList + ChatInput)
│
└── main.tsx                   # Точка входа
```

### Ключевые модули

#### `entities/message`
- **`model/message.store.ts`** — Zustand store для управления сообщениями
- **`ui/MessageList.tsx`** — Список сообщений с автоскроллом
- **`ui/MessageItem.tsx`** — Отдельное сообщение (мемоизировано)
- **`ui/components/MessageContent.tsx`** — Рендеринг контента с Markdown
- **`ui/components/CodeBlock.tsx`** — Компонент для code blocks
- **`lib/utils/markdown-parser.util.ts`** — Парсер Markdown (не блокирующий main thread)
- **`hooks/use-auto-scroll.hook.ts`** — Автоскролл с отключением при ручном скролле

#### `features/chat`
- **`hooks/streaming.hook.ts`** — Логика потоковой генерации
- **`hooks/chat-form-handlers.hook.ts`** — Обработчики формы (submit, keyboard)
- **`services/chat-stream.service.ts`** — Сервис эмуляции стриминга
- **`lib/utils/text-generator.util.ts`** — Генератор текста/кода
- **`ui/ChatInput.tsx`** — Поле ввода с автоизменением высоты
- **`ui/components/ChatInputButton.tsx`** — Кнопка Generate/Stop

## ✅ Реализованные требования из ТЗ

### Функциональные требования

#### ✅ Список сообщений
- **Реализация**: `entities/message/ui/MessageList.tsx`
- **Особенности**:
  - Поддержка 100+ сообщений
  - Оптимизированный рендеринг через мемоизацию
  - Каждое сообщение мемоизировано для предотвращения лишних рендеров

#### ✅ Эмуляция стриминга
- **Реализация**: `features/chat/services/chat-stream.service.ts`
- **Особенности**:
  - Потоковое добавление текста в последнее сообщение
  - Скорость: чанки каждые 15мс (настраивается)
  - Объем: ~10,000 слов в одном сообщении
  - Поддержка типов: `text` и `code`

#### ✅ Управление состоянием
- **Реализация**: Zustand stores
  - `entities/message/model/message.store.ts` — сообщения
  - `features/chat/model/chat.store.ts` — состояние генерации
- **Особенности**:
  - Все сообщения хранятся в Zustand
  - Оптимизированные селекторы для предотвращения лишних подписок

### UI/UX требования

#### ✅ Автоскролл к низу
- **Реализация**: `entities/message/hooks/use-auto-scroll.hook.ts`
- **Особенности**:
  - Автоматический скролл при генерации
  - Отключается, если пользователь скроллит вверх
  - Включается обратно при возврате вниз
  - Использует `requestAnimationFrame` для плавности

#### ✅ Кнопка "Stop Generating"
- **Реализация**: `features/chat/ui/components/ChatInputButton.tsx`
- **Особенности**:
  - Меняется на "Stop" во время генерации
  - Останавливает стриминг и сохраняет накопленные chunks

### Технические требования

#### ✅ Zero UI Freezes (60 FPS)
**Решения**:

1. **Мемоизация компонентов**:
   - `MessageItem` обернут в `memo` с оптимизированным сравнением
   - `MessageContent` мемоизирован
   - Использование `useMemo` для тяжелых вычислений

2. **Оптимизированные селекторы Zustand**:
   - Вместо `state => state` используются конкретные селекторы
   - Компоненты подписываются только на нужные части store

3. **Батчинг обновлений**:
   - Chunks добавляются через Zustand, который батчит обновления
   - Использование `requestAnimationFrame` для скролла

4. **Разделение ответственности**:
   - Логика стриминга вынесена в отдельный сервис
   - UI компоненты остаются легкими

#### ✅ Эффективный рендеринг
**Решения**:

1. **Умная мемоизация**:
   - Парсинг Markdown выполняется только при изменении контента
   - Результаты парсинга кешируются через `useMemo`

2. **Оптимизированное сравнение пропсов**:
   - `MessageItem` сравнивает только необходимые поля
   - Для стриминга сравнивается только последний chunk

3. **Ленивый рендеринг**:
   - Markdown парсится только для видимых сообщений
   - Компоненты рендерятся только при изменении данных

#### ✅ Markdown поддержка
**Реализация**: `entities/message/lib/utils/markdown-parser.util.ts`

**Поддерживаемые элементы**:
- **Code blocks**: ` ```code``` `
- **Inline code**: `` `code` ``
- **Bold text**: `**bold**`

**Оптимизации**:
- Легковесный парсер на регулярных выражениях
- Для больших текстов (>10KB) используется `requestIdleCallback`
- Парсинг не блокирует main thread
- Результаты кешируются через `useMemo`

**Стилизация**:
- Code blocks имеют темный фон как в IDE (`#1e1e1e`)
- Поддержка темной темы
- Правильный перенос строк для длинного кода

## 🔧 Технические детали

### Потоковая генерация

```typescript
// features/chat/services/chat-stream.service.ts
// Эмуляция стриминга через setInterval
// Генерирует чанки каждые 15мс до достижения targetWordsCount
```

**Процесс**:
1. Создается сообщение с `isDone: false`
2. `ChatStreamService` генерирует массив чанков заранее
3. Через `setInterval` отправляет чанки в store
4. При завершении все chunks объединяются в `content`

### Автоскролл

```typescript
// entities/message/hooks/use-auto-scroll.hook.ts
// Отслеживает изменения в chunks и автоматически скроллит
```

**Логика**:
- Отслеживает количество сообщений и chunks последнего сообщения
- Проверяет, находится ли пользователь внизу (threshold: 100px)
- Отключается при ручном скролле вверх
- Использует двойной `requestAnimationFrame` для гарантированного скролла

### Markdown парсинг

```typescript
// entities/message/lib/utils/markdown-parser.util.ts
// Парсит текст на токены: text, code, bold
```

**Алгоритм**:
1. Находит все совпадения (code blocks, inline code, bold)
2. Сортирует по позиции
3. Строит массив токенов
4. Для больших текстов использует асинхронный парсинг

### Оптимизация рендеринга

**Стратегии**:
1. **Мемоизация на уровне компонентов**: `React.memo` с кастомным сравнением
2. **Мемоизация вычислений**: `useMemo` для парсинга Markdown
3. **Селекторы Zustand**: Подписка только на нужные данные
4. **Батчинг**: Zustand автоматически батчит обновления

## 📦 Зависимости

### Основные
- **React 19.2.0** — UI библиотека
- **TypeScript 5.9.3** — Типизация
- **Zustand 5.0.10** — State management
- **Vite 7.2.4** — Сборщик
- **Tailwind CSS 4.1.18** — Стилизация

### UI компоненты
- **@radix-ui/react-slot** — Примитивы
- **lucide-react** — Иконки
- **class-variance-authority** — Утилиты для классов
- **tailwind-merge** — Объединение Tailwind классов

## 🎨 Стилизация

Проект использует Tailwind CSS с кастомной конфигурацией:
- Темная тема для code blocks (`#1e1e1e`)
- Адаптивный дизайн
- Плавные анимации
- Поддержка dark mode

## 🧪 Тестирование

Для тестирования производительности:
1. Запустите генерацию сообщения
2. Откройте DevTools → Performance
3. Проверьте FPS во время генерации
4. Убедитесь, что нет блокировок main thread

## 📝 Примечания

- Генератор текста создает реалистичный код с переносами строк
- Markdown парсер оптимизирован для больших объемов
- Все компоненты мемоизированы для производительности
- Автоскролл работает плавно даже при быстрой генерации

## 🔮 Возможные улучшения

- [ ] Виртуализация списка сообщений (react-window/react-virtuoso)
- [ ] Web Workers для парсинга Markdown больших текстов
- [ ] Debounce для автоскролла при очень быстрой генерации
- [ ] Поддержка большего количества Markdown элементов
- [ ] Синтаксическая подсветка кода (Prism.js/Highlight.js)

## 📄 Лицензия

Проект создан в рамках тестового задания.
